# `rt/interrupt.h`

**Audience:** application.

## Functions

### `rt_interrupt_is_active`

```c
bool rt_interrupt_is_active(void);
```

- **Blocking behavior:** Non-blocking (or read-only)
- **ISR:** Query only.

## See also

- Kernel feature docs under `03-Kernel-Features/` when applicable
- Source: `include/rt/interrupt.h`
